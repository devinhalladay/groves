import { NextPage } from 'next';

const ChannelList: NextPage = (props) => {
  console.log(props);
  
  return (
    // props.channels.map(channel => 
    //   <div key={channel.id}>{channel.title}</div>
    // )
    <div>test</div>
  )

}