export default class Block {
  create = (token, channelSlug, content) => {
    // TODO: Create block - read API docs
  }

  delete = (token, blockURI) => {
    // TODO: Delete block - read api docs
  }

  update = (token, blockURI, content) => {
    // TODO: Decide whether or not to update blocks via groves
  }
}